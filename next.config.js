/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // typedRoutes disabled temporarily to unblock build (Vercel was building older commit without all pages)
}

module.exports = nextConfig
